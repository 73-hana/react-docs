import ReactDOM from "react-dom";
import ComponentProps01 from "./components/ComponentProps01";

const app = (
  <>
    <ComponentProps01 />
  </>
);

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(app);